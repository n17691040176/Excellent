本地启动与联调记录（2026-09-10）

> 当前运行方式已切换为完整 Docker（五个健康容器），访问端口和测试数据库保持一致。最新启动命令见 [本地 Docker 说明](docker_local_20260910.md)。

> 状态更新：本文为首次启动及修复前复现记录。问题已修复，服务已重启，后续验证与当前行为见 [修复与验收记录](fix_20260910_city_partner.md)。测试库新增了修复验证数据，以下初始数据数量不代表当前数量。

三个应用已启动，MySQL 和 Redis 使用独立 Docker Compose 项目 `excellent-localtest`。所有监听地址均为 127.0.0.1。

| 服务 | 地址 |
| --- | --- |
| 后台管理 | http://127.0.0.1:5173/login |
| 移动端 H5 | http://127.0.0.1:5174/#/pages/home/index |
| 后端接口文档 | http://127.0.0.1:8000/docs |
| 健康检查 | http://127.0.0.1:8000/health |
| MySQL | 127.0.0.1:13306，数据库 excellent_local_test |
| Redis | 127.0.0.1:16379 |

后台初始化账号：`18800000000`，密码 `Admin@123`。

移动端测试用户：`19900134224`。在登录页点击“获取验证码”，本地验证码会自动填入，勾选协议后即可登录。此环境禁用了真实短信服务。该用户也设置了供密码接口测试的密码 `LocalTest123`。

支付为项目已有的模拟模式，微信、支付宝真实渠道和云存储关闭。本地库已切换为 CITY_PARTNER 模式，包含测试分类、商品、地址、用户、退款订单及两个城市席位；没有导入线上业务数据，商品暂无图片。

再次启动可在项目根目录运行：

```powershell
.\tools\start-local-test.ps1
```

脚本依赖本机 Python/Node、已安装的项目依赖及运行中的 Docker Desktop。脚本将运行参数传递给后台进程，不改写项目原来的 `.env`。端口已经存在监听时会保留该进程。日志位于项目 `logs/local-test/`；数据库数据保存在独立的 `excellent-localtest_mysql_localtest` Docker 卷。

实测通过：

- 后端启动完成，健康检查返回 MySQL、Redis 均为 `ok`。
- Chrome 浏览器后台登录成功，数据概览、商品列表、佣金页面正常加载。
- Chrome 手机尺寸浏览器首页正常显示测试商品，验证码登录与个人中心正常。
- 浏览器页面检查没有捕获到脚本异常；后台页面及移动端首页检查没有发现 HTTP 400 及以上响应。
- HTTP 接口完成模式切换、分类和商品创建、商品提审及审核上架、规则保存、用户注册、地址创建、下单、模拟支付、席位购买和本地退款。

本次通过真实 MySQL + HTTP 接口确认的业务缺陷：

| 操作 | 实际结果 |
| --- | --- |
| 商品 3 开启城市合伙人分润，金额设为 100 元 | 数据库保存为开启、100 元，接口回显为关闭、0 元 |
| 订单 1 支付 1,000 元后，分别购买席位 1 和 2 | 两次都成功，两个席位使用同一 `order_id=1` |
| 对订单 1 执行模拟退款 | 订单为 REFUNDED / REFUND，两个席位仍由用户 3 持有，轮换流水仍为 SUCCESS |
| 查询退款订单 1 的公司尾差 | 600 元 COMPANY_REMAINDER 流水仍为 FROZEN |
| 查询后台商品分润规则 | 新规则已保存，但列表返回空；后台页面仍显示原模式 |

上述结果是本地模拟支付与实际数据库的联调结果；没有执行真实支付渠道扣款/退款，也未执行并发压力测试。业务实现未修改。

调试产物位于 `logs/local-test/`：`api-results.json`、`browser-results.json`、`mobile-login-results.json`，以及后台和移动端页面截图。完整代码审查结论见同目录下的 `review_20260910_city_partner.md`。
